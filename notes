1. if a seperate folder for hugo is added, it is not detected in github actions
2. Themes folder has to be committed properly with all contents 
3. default branch is gh-pages 
4. public folder should be generated correctly
5. config.toml should have proper base URL

random commit ...
